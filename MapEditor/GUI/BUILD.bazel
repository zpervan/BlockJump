cc_library(
    name = "gui",
    srcs = glob(["src/*.cpp"]),
    hdrs = glob(["src/*.h"]),
    visibility = ["//visibility:public"],
    deps = [
        "@imguilib//:imgui",
        "@spdloglib//:spdlog",
    ],
)
