syntax = "proto3";

option go_package = "./proto";

message Position
{
    float x = 1;
    float y = 2;
}

message Orientation
{
    float angle = 1;
}

message EntitySnapshot
{
    string id = 1;
    string name = 2;
    int32 health = 3;
    Position position = 4;
    Orientation orientation = 5;
}

/// @TODO: Consider to add NPCs in the future
message WorldSnapshot
{
    string id = 1;
    repeated EntitySnapshot entities = 2;
}